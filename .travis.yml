language: shell

git:
  depth: 2

install:
  - mv -v ../dot-files "${HOME}/.dotfiles"
script:
  - ls -a "${HOME}/.dotfiles/"
  - bash "${HOME}/.dotfiles/.dotfiles_setup.sh" install

after_script:
  - bash "${HOME}/.dotfiles/.dotfiles_setup.sh" test
