language: shell

git:
  depth: 2

install:
  - ls -thora
  - rsync -uar --delete-after {.,}* "${HOME}"
  - ls -thora "${HOME}"

after_script:
  - bash .dotfiles_setup.sh install

after_script:
  - bash .dotfiles_setup.sh test
