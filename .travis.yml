language: shell

git:
  depth: 2

notifications:
  email: change

install:
  - rsync -uar --delete-after {.,}* "${HOME}" && cd "${HOME}"

script:
  - bash -x "${HOME}/.dotfiles/.dotfiles_setup.sh" install

after_script:
  - bash -x "${HOME}/.dotfiles/.dotfiles_setup.sh" test
